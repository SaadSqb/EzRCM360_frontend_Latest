# EzRCM360 Environment Configuration
# Copy this file to .env.local and adjust values for your environment.
#
# Environment detection:
# - npm run dev → uses LOCAL defaults (NODE_ENV=development)
# - npm run build && npm run start → uses PRODUCTION defaults (NODE_ENV=production)
# - Set NEXT_PUBLIC_APP_ENV=production or =local to override
# =============================================================================

# -----------------------------------------------------------------------------
# API
# -----------------------------------------------------------------------------
# Backend API base URL (no trailing slash).
# Local:  https://localhost:5001
# Production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=https://localhost:5001

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
# App name shown in header / branding
NEXT_PUBLIC_APP_NAME=EzRCM360

# App URL for redirects (optional; defaults to window.location.origin in browser)
# NEXT_PUBLIC_APP_URL=https://settings.yourdomain.com

# -----------------------------------------------------------------------------
# Force environment (optional)
# Use 'production' or 'local' to override NODE_ENV-based detection
# NEXT_PUBLIC_APP_ENV=local
# NEXT_PUBLIC_APP_ENV=production

# -----------------------------------------------------------------------------
# Auth storage keys (optional; change if you need different localStorage keys)
# NEXT_PUBLIC_AUTH_TOKEN_KEY=accessToken
# NEXT_PUBLIC_REFRESH_TOKEN_KEY=refreshToken
