import { PageHeader } from "@/components/settings/PageHeader";
import { Card } from "@/components/ui/Card";

export default function EntityFeeSchedulesPage() {
  return (
    <div>
      <PageHeader title="Entity Billable Fee Schedules" description="Entity-level fee schedule assignments." />
      <Card><p className="text-sm text-slate-600">Placeholder. Wire to backend when available.</p></Card>
    </div>
  );
}
